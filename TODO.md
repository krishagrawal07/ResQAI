# Run App on Phone - Progress (COMPLETE)

## Steps:
- [x] 1. Install dependencies (npm install in ResQAITemp/)
- [x] 2. Run npx react-native run-android
- [x] 3. App installed and launched on 'moto g85 5G - 15' (BUILD SUCCESSFUL)

Note: App running. "Unable to load script" indicates Metro bundler (localhost:8081) issue - common on real device. Run `cd ResQAITemp && npx react-native start` in new terminal to restart Metro, shake phone (Cmd+M/DevMenu > Reload), enable 'Hot Reloading', or toggle Airplane mode/WiFi.






